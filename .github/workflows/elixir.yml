name: Unit Tests

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:12-alpine
        env:
          POSTGRES_HOST: study_manager
          POSTGRES_PASSWORD: letmein
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    container:
      image: elixir:1.9-alpine

    steps:
    - uses: actions/checkout@v2
    - name: Install Dependencies
      run: |
        mix local.rebar --force
        mix local.hex --force
        mix deps.get
    - name: Create Test Database
        run: mix ecto.setup
        env:
          POSTGRES_HOST: study_manager
          POSTGRES_PASSWORD: letmein
          POSTGRES_PORT: 5432
    - name: Run Tests
      run: mix test
