<h1>New Study session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.study_session_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.study_session_path(@conn, :index) %></span>
