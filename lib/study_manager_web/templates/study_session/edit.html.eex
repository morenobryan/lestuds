<h1>Edit Study session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.study_session_path(@conn, :update, @study_session)) %>

<span><%= link "Back", to: Routes.study_session_path(@conn, :index) %></span>
