<h1>New Availability</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.availability_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.availability_path(@conn, :index) %></span>
